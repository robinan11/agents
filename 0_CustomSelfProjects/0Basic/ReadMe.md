#  If the Project Wasn't Set Up Yet

If you're starting a brand new project from scratch (which is rare in your course), you can initialize a uv project by running:

```bash
uv init

```



Run using uv run my_script.py








# agents folder was copied using

> git clone https://github.com/robinan11/agents.git

